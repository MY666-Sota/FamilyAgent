# FamilyAgent 家庭 AI 服务器 — 架构设计文档

> 版本：v3.0（最终版，基于 2026 年中 SOTA 实践与成熟开源组件）
> 日期：2026年6月
> 状态：设计定稿
> 说明：本版废弃 v1.0 / v2.0 全部内容。架构基于实际调研，所有核心组件优先复用 GitHub 成熟开源项目，不重造轮子。

---

## 0. 设计依据（调研结论）

本架构的每个技术选型都有调研支撑，核心结论：

| 决策点 | 选择 | 依据 |
|--------|------|------|
| 工具接入标准 | **MCP** | 已成事实标准（"AI 界 USB-C"），交由 Linux Foundation 治理，全厂商支持 |
| 编排框架 | **LangGraph** | 有状态、可审计、支持人工审批节点，生产级首选 |
| 记忆层 | **Mem0** | 挂载式记忆层，集成成本最低，最流行（~48K stars）|
| 知识检索 | **混合 RAG（传统为主 + Agentic 按需）** | 全程 Agentic 成本/延迟翻 3–10 倍，简单查询不划算 |
| 工具调用 | **Function Calling + MCP 双层** | 底层 function calling，接入层 MCP，换模型不重写工具 |
| 不采用 | A2A、AutoGen、Swarm | A2A 是企业级跨厂商通信属过度工程；后两者已停更/归档 |

---

## 1. 项目概述

### 1.1 目标

家庭台式机私有部署 AI 服务，家庭成员通过企业微信发送指令，系统自动完成 PPT 制作、作业批改、文档写作、学习计划、知识库问答等任务，具备个性化长期记忆，结果发回用户。所有数据本地化，隐私可控。

### 1.2 核心原则

- **数据私有**：家庭数据全部本地存储，不出家门
- **复用优先**：核心能力优先用 GitHub 成熟开源项目，自研只做"胶水"
- **标准化**：以 MCP 为工具接入标准，保证未来兼容性与可移植性
- **可演进**：渠道、Agent、工具均可扩展，核心架构稳定
- **低成本**：硬件一次性投入，API 月均 ¥15–40

---

## 2. 整体架构

### 2.1 分层架构图

```
┌────────────────────────────────────────────────────────────────┐
│                          接入层                                 │
│   企业微信   [预留]公众号   [预留]网页   [预留]小程序            │
│   └─ 复用开源：chatgpt-on-wechat（企微/公众号/飞书多渠道）       │
└───────────────────────────┬────────────────────────────────────┘
                            │ 标准消息
┌───────────────────────────▼────────────────────────────────────┐
│                  LangGraph 编排层（大脑）★ 核心                  │
│                                                                │
│  StateGraph 状态机驱动：                                        │
│   [1] 输入 Guardrails（儿童内容安全）                           │
│   [2] 意图理解节点（LLM 分类，非关键词）                        │
│   [3] 路由决策（单任务直达 / 复杂任务进编排子图）               │
│   [4] Agent 节点调度（条件边 + 并行分支）                       │
│   [5] 人工审批节点（HITL，敏感操作可选）                        │
│   [6] 结果合并 + 输出 Guardrails                                │
│                                                                │
│  全程状态可持久化、可回溯、可中断恢复                            │
└──────┬────────────┬───────────┬───────────┬────────────────────┘
       │            │           │           │
   ┌───▼───┐   ┌────▼────┐  ┌───▼───┐  ┌────▼─────┐
   │ PPT   │   │ 作业批改│  │文档写作│  │学习计划/  │   ← Agent 节点
   │ Agent │   │ Agent   │  │ Agent │  │问答 Agent │
   └───┬───┘   └────┬────┘  └───┬───┘  └────┬─────┘
       │            │           │           │
┌──────▼────────────▼───────────▼───────────▼────────────────────┐
│                    MCP 工具层 ★ 标准基座                        │
│  通过 MCP 协议统一接入工具，换模型/换框架工具不重写              │
│                                                                │
│  ├─ Office-Word-MCP-Server   → 生成/编辑 .docx                 │
│  ├─ mcp-pandoc               → 格式转换                        │
│  ├─ filesystem-mcp           → 文件读写                        │
│  ├─ markitdown-mcp           → 文档→Markdown 预处理            │
│  ├─ web-search-mcp           → 联网检索（按需）                │
│  └─ [自研] presenton-mcp     → 封装 PPT 生成                   │
└──────┬─────────────────────────────────────────────────────────┘
       │
┌──────▼──────────────────────────┬──────────────────────────────┐
│      Mem0 记忆层 ★              │       RAG 检索层 ★            │
│                                 │                              │
│  长期记忆（挂载式）：           │  默认：传统 RAG（快/省）      │
│   - 用户画像（年级/偏好）       │   混合检索 + Reranker        │
│   - 错题积累                    │  按需：Agentic RAG 循环      │
│   - 学习偏好/历史               │   （复杂多跳查询才触发）      │
│                                 │  引擎：RAGFlow / Dify 知识库 │
└──────┬──────────────────────────┴──────────────┬───────────────┘
       │                                         │
┌──────▼─────────────────────────────────────────▼───────────────┐
│                          模型层                                 │
│  对话/推理：DeepSeek V3 / R1（API）或本地 Ollama                │
│  视觉识别：Qwen-VL（作业图片）                                  │
│  文档OCR：PaddleOCR-VL（手写体识别）                            │
│  Embedding+Reranker：Xinference 托管（Qwen3-Embedding/Reranker）│
└────────────────────────────────────────────────────────────────┘
```

### 2.2 自研 vs 复用边界

```
复用开源（占 80%）：
  入口        → chatgpt-on-wechat
  PPT 生成    → Presenton（或 PptxGenJS 做底层）
  作业 OCR    → PaddleOCR-VL + MarkItDown
  文档生成    → Office-Word-MCP-Server + mcp-pandoc
  知识库      → RAGFlow / Dify
  模型托管    → Ollama + Xinference
  记忆        → Mem0
  现成能力    → awesome-agent-skills 选取

自研（占 20%，仅胶水层）：
  LangGraph 编排逻辑（StateGraph 定义）
  意图理解 + 路由节点
  Guardrails 节点
  presenton-mcp（把 PPT 生成包成 MCP 工具）
  渠道适配器（未来扩展时写）
```

---

## 3. 编排层设计（LangGraph）★

### 3.1 为什么是 LangGraph 而非关键词路由

```
v1.0 关键词匹配的问题：
  "给我整理个演示用的东西" → 无"PPT"关键词 → 路由失败

LangGraph 方案：
  状态机 + LLM 意图节点，理解任意自然表达
  且支持有状态：记住任务进度、中断恢复、人工审批
```

### 3.2 StateGraph 状态定义

```python
# 编排状态（贯穿整个流程）
class FamilyAgentState(TypedDict):
    channel: str            # 来源渠道
    user_id: str            # 统一用户标识
    raw_input: str          # 原始输入
    msg_type: str           # text/image/file
    media_url: str          # 媒体地址
    intent: str             # 识别出的意图
    params: dict            # 任务参数
    subtasks: list          # 复杂任务的子任务列表
    memory_context: dict    # 从 Mem0 取回的记忆
    rag_context: str        # 从 RAG 取回的知识
    result: dict            # 各 Agent 结果
    need_approval: bool     # 是否需人工审批
    final_output: dict      # 最终输出
```

### 3.3 节点流转图

```
START
  │
  ▼
input_guardrail ──(不通过)──▶ friendly_reject ──▶ END
  │(通过)
  ▼
load_memory（Mem0 取用户画像/历史）
  │
  ▼
classify_intent（LLM 意图分类）
  │
  ├─(单任务)──▶ route_to_agent ──▶ [对应 Agent 节点]
  │                                      │
  └─(复杂任务)─▶ plan_subtasks ──▶ [多 Agent 编排子图]
                                         │
                                         ▼
                                  merge_results
                                         │
                                         ▼
                              need_approval? ─(是)─▶ human_approval
                                         │(否)            │
                                         ▼◀───────────────┘
                                  output_guardrail
                                         │
                                         ▼
                                  save_memory（Mem0 写回）
                                         │
                                         ▼
                                        END
```

### 3.4 复杂任务编排示例

```
用户："分析孩子这周错题，做一份针对性复习PPT"

classify_intent → intent=multi
plan_subtasks → [
    {agent: homework, action: 检索本周错题+分析薄弱点},
    {agent: ppt, action: 基于薄弱点生成PPT, depends_on: 上一步}
]
串行执行 → merge → 输出一个 PPT 文件
```

---

## 4. MCP 工具层设计 ★

### 4.1 为什么用 MCP

```
传统做法：每个工具写一套对接代码，换模型/框架要重写
MCP 做法：工具实现一次，任何支持 MCP 的模型/框架都能用

收益：
  - 换对话模型（DeepSeek↔Claude↔GPT）工具层零改动
  - 生态现成：1300+ MCP server 可直接接入
  - 标准化：未来新工具按 MCP 规范接入即可
```

### 4.2 采用的 MCP Server（全部开源现成）

| MCP Server | 来源 | 用途 |
|-----------|------|------|
| Office-Word-MCP-Server | GongRzhe | 生成/编辑 Word 文档 |
| mcp-pandoc | vivekVells | 文档格式互转 |
| filesystem-mcp | mark3labs | 本地文件读写 |
| markitdown-mcp | microsoft | PDF/图片/Office → Markdown |
| web-search-mcp | 社区 | 联网检索（按需开启）|
| **presenton-mcp** | 自研 | 把 Presenton 的 PPT 生成包成 MCP 工具 |

清单参考：punkpeye/awesome-mcp-servers、fuzzylabs/awesome-secure-mcp-servers（安全审计版）

### 4.3 Function Calling 与 MCP 的关系

```
模型层：Function Calling（模型按 JSON schema 调用函数）
   ↓ 底层触发机制
接入层：MCP（标准化工具的发现/暴露/调用/管理）

两者配合，不是替代：MCP 在底层仍用 function calling 触发
```

---

## 5. Agent 设计（复用开源能力）

| Agent | 复用的开源组件 | 模型 | 用记忆 | 用RAG |
|-------|--------------|------|-------|-------|
| PPT 制作 | **Presenton**（开箱）/ PptxGenJS（底层）| DeepSeek V3 | 否 | 可选 |
| 作业批改 | **PaddleOCR-VL** + MarkItDown | Qwen-VL + DeepSeek R1 | ✅写 | ✅读 |
| 文档写作 | **Office-Word-MCP** + mcp-pandoc | DeepSeek V3 | 否 | 可选 |
| 学习计划 | 自研 LangGraph 节点 | DeepSeek V3 | ✅读 | ✅读 |
| 知识问答 | **RAGFlow / Dify 知识库** | DeepSeek V3 | ✅读 | ✅读 |

### 5.1 作业批改链路（重点）

```
孩子拍照上传作业
   │
   ▼
PaddleOCR-VL（2026.1 开源，支持手写体识别）
   │ 图片 → 结构化文字
   ▼
MarkItDown（统一转 Markdown）
   │
   ▼
DeepSeek R1（分步骤解题，判对错）
   │
   ▼
Mem0 写入错题记录（科目/知识点/错误描述）
   │
   ▼
结果发回企微
```

### 5.2 复用现成 Skills

从以下仓库选取生产级 Skills 直接复用，避免重造：
- VoltAgent/awesome-agent-skills（跨 Agent 兼容）
- seb1n/awesome-ai-agent-skills（70+ 生产级技能）
- Aidenwu0209/PaddleOCR-Skills（OCR/文档解析，对接作业批改）

---

## 6. Mem0 记忆层设计 ★

### 6.1 为什么是 Mem0

```
家庭助手需要记住：孩子年级、学习偏好、历史错题
Mem0 是"挂载式记忆层"，集成成本最低，挂到 LangGraph 上即可
（备选：Zep 适合"事实随时间变化"场景；Letta 过重，不选）
```

### 6.2 记忆分层

```
短期记忆：当次对话上下文（LangGraph state + checkpointer）
长期记忆：Mem0 托管
   ├─ 用户画像：{name, grade, preferences}
   ├─ 错题积累：批改后自动写入
   └─ 历史任务：用于个性化
按 user_id 隔离，每个家庭成员独立
```

### 6.3 记忆使用流程

```
请求进来 → load_memory 节点从 Mem0 取回该用户记忆
         → 注入到 Agent 上下文
任务完成 → save_memory 节点写回新记忆（如新错题）

效果：
  制定学习计划时自动结合历史薄弱点
  问答时按孩子年级调整难度
```

---

## 7. RAG 检索层设计 ★

### 7.1 混合策略（核心决策）

```
默认走传统 RAG（90% 场景）：
  快、便宜、可溯源
  适合：个人文档、笔记、常见问答

仅复杂查询触发 Agentic RAG：
  检索→推理→再检索的控制循环
  适合：多跳问题、模糊查询
  代价：token×3-10、延迟×2-5，所以按需触发

判断逻辑由 LangGraph 意图节点决定
```

### 7.2 传统 RAG 的检索增强

```
查询
  ├─→ BM25 关键词检索（精确匹配）
  └─→ 向量检索（语义相似，Qwen3-Embedding）
        │
        ▼
   合并去重
        │
        ▼
   Reranker 重排（Qwen3-Reranker，Xinference 托管）
        │
        ▼
   Top-K → 喂给模型
```

### 7.3 知识库引擎选型

```
起步：Dify 内置知识库（最省事）
进阶：RAGFlow（深度文档理解，知识库要求高时上）
Embedding/Reranker：Xinference 统一托管
```

---

## 8. Guardrails 设计 ★

### 8.1 输入护栏（LangGraph 首节点）

```
检查：内容安全 / 场景边界 / 提示词注入防护
不通过 → 友好引导："我们专注学习哦，有什么作业问题吗？"
```

### 8.2 输出护栏（LangGraph 末节点）

```
检查：内容审查 / 格式校验（PPT/文档是否生成成功）/ 长度控制
失败 → 重试一次 → 仍失败降级为文字说明
```

### 8.3 人工审批（HITL）

LangGraph 原生支持 human-in-the-loop 节点，敏感操作（如删除知识库）可插入家长审批环节。

---

## 9. 网络架构

```
家里 WiFi：手机 → 局域网IP → 服务
外出：手机企微 → 企微服务器 → Cloudflare Tunnel（免费子域名）→ 台式机

无需购买域名，Cloudflare 提供免费 HTTPS 子域名
```

### 9.1 端口规划

| 服务 | 端口 | 说明 |
|------|------|------|
| Nginx | 80 | 统一入口 |
| chatgpt-on-wechat | 8080 | 企微接入 |
| LangGraph 编排服务 | 8081 | 自研核心 |
| Dify / RAGFlow | 5001 | 知识库 |
| Mem0 服务 | 8082 | 记忆层 |
| Xinference | 9997 | Embedding/Reranker |
| Ollama | 11434 | 本地 LLM（可选）|
| PaddleOCR 服务 | 8868 | 作业 OCR |
| PostgreSQL | 5432 | 数据 |
| Redis | 6379 | 缓存/队列 |
| 各 MCP Server | 动态 | stdio/SSE |

---

## 10. 部署组件（docker-compose）

```
docker-compose.yml
├── nginx                  反向代理
├── chatgpt-on-wechat      企微入口（开源）
├── langgraph-orchestrator 编排核心（自研）
├── dify / ragflow         知识库（开源，二选一起步）
├── mem0                   记忆层（开源）
├── xinference             Embedding/Reranker（开源）
├── ollama                 本地LLM（开源，可选）
├── paddleocr-server       作业OCR（开源）
├── presenton              PPT生成（开源）
├── mcp-servers/           各MCP工具（开源+自研presenton-mcp）
├── postgresql             数据+记忆持久化
└── redis                  缓存/队列

独立运行：
└── cloudflared            内网穿透（免费）
```

---

## 11. 成本估算

| 项目 | 费用 | 说明 |
|------|------|------|
| 台式机硬件 | ¥3,000–3,500 | 多个本地服务（OCR/Embedding/Reranker），建议 32GB 内存 |
| 全部软件 | ¥0 | 全开源 |
| 企微 / Cloudflare | ¥0 | 免费 |
| 本地模型（OCR/Embedding/Rerank）| ¥0 | 本地运行 |
| DeepSeek API（对话/推理）| ¥15–40/月 | 含意图识别+Guardrails 调用 |
| 电费 | ¥40–60/月 | 多服务常驻 |
| **合计** | **¥55–100/月** | |

---

## 12. 部署顺序

```
阶段一：基础环境（1小时）
  □ 装 Docker Desktop
  □ 部署 Ollama + Xinference（本地模型托管）

阶段二：核心服务（2小时）
  □ 部署 chatgpt-on-wechat（企微入口）
  □ 部署 Dify 或 RAGFlow（知识库）
  □ 部署 Mem0（记忆层）
  □ 配置 DeepSeek API

阶段三：编排层（自研，2-3小时）
  □ 编写 LangGraph StateGraph
  □ 实现意图/路由/Guardrails 节点
  □ 接入各 MCP Server

阶段四：能力组件（按需）
  □ PaddleOCR-VL 服务（作业批改）
  □ Presenton + presenton-mcp（PPT）
  □ Office-Word-MCP（文档）

阶段五：外网与测试
  □ Cloudflare Tunnel 配置
  □ 企微回调验证
  □ 各功能端到端测试
```

---

## 13. 演进路线图

| 方向 | 实现方式 | 优先级 |
|------|---------|--------|
| 错题周报 | LangGraph 定时任务 + Office-Word-MCP 生成 | 高 |
| 渠道扩展（公众号/网页）| chatgpt-on-wechat 已支持多渠道，配置即可 | 中 |
| 新增 Agent | LangGraph 加节点 + 选用 awesome-agent-skills | 中 |
| 新增工具 | 接入新的 MCP Server（生态 1300+）| 持续 |
| 语音交互 | 企微语音 → Whisper → 现有流程 | 中 |
| Agentic RAG 深化 | 复杂查询场景扩展控制循环 | 低 |
| 模型升级 | 对话模型随时换（MCP 工具层不变）| 持续 |

---

## 附录 A：完整技术栈与开源组件清单

| 层次 | 组件 | 类型 | GitHub |
|------|------|------|--------|
| 入口 | chatgpt-on-wechat | 开源 | zhayujie/chatgpt-on-wechat |
| 编排 | LangGraph | 开源框架 | langchain-ai/langgraph |
| 工具标准 | MCP | 协议 | modelcontextprotocol |
| 记忆 | Mem0 | 开源 | mem0ai/mem0 |
| 知识库 | RAGFlow / Dify | 开源 | infiniflow/ragflow · langgenius/dify |
| 模型托管 | Ollama + Xinference | 开源 | ollama · xorbitsai/inference |
| 作业OCR | PaddleOCR-VL | 开源 | PaddlePaddle/PaddleOCR |
| 文档预处理 | MarkItDown | 开源 | microsoft/markitdown |
| PPT生成 | Presenton / PptxGenJS | 开源 | presenton/presenton · gitbrent/PptxGenJS |
| Word生成 | Office-Word-MCP-Server | 开源MCP | GongRzhe/Office-Word-MCP-Server |
| 格式转换 | mcp-pandoc | 开源MCP | vivekVells/mcp-pandoc |
| 文件操作 | filesystem-mcp | 开源MCP | mark3labs/mcp-filesystem-server |
| 内网穿透 | cloudflared | 免费 | cloudflare/cloudflared |
| 能力复用 | awesome-agent-skills | 开源集合 | VoltAgent/awesome-agent-skills |

## 附录 B：明确不采用的技术（及原因）

| 不采用 | 原因 |
|--------|------|
| A2A 协议 | 企业级跨厂商 agent 通信，家庭单系统场景属过度工程 |
| AutoGen | 已进入维护模式，不建议新项目 |
| OpenAI Swarm | 已归档 |
| 全程 Agentic RAG | 成本/延迟翻 3–10 倍，简单查询不划算，改为按需触发 |
| 关键词意图匹配（v1.0）| 脆弱，换种说法即失效，改用 LLM 意图分类 |
| WeChatFerry（hook 个人微信）| 封号/合规风险，改用企业微信官方接口 |

## 附录 C：项目文件结构

```
FamilyAgent/
├── 架构设计文档.md              ← 本文件
├── docker-compose.yml
├── .env
├── orchestrator/                ★ 自研 LangGraph 编排
│   ├── graph.py                 ← StateGraph 定义
│   ├── nodes/
│   │   ├── intent.py            ← 意图理解节点
│   │   ├── router.py            ← 路由节点
│   │   ├── guardrails.py        ← 护栏节点
│   │   ├── memory.py            ← Mem0 读写节点
│   │   └── agents.py            ← Agent 调度节点
│   └── mcp_clients.py           ← MCP 工具客户端
├── mcp-servers/
│   └── presenton-mcp/           ★ 自研：PPT生成MCP封装
├── nginx/nginx.conf
└── cloudflared/config.yml
```

## 附录 D：调研来源

架构选型基于以下调研：
- MCP 采用现状：digitalapplied.com/blog/mcp-adoption-statistics-2026
- A2A 现状：credal.ai/blog/what-happened-to-a2a-protocol
- Agentic RAG：marsdevs.com/guides/agentic-rag-2026-guide
- 编排框架对比：digitalapplied.com/blog/open-source-agent-frameworks-5-compared-2026
- Agent 记忆：mem0.ai/blog/state-of-ai-agent-memory-2026
- 开源平台对比：jimmysong.io/blog/open-source-ai-agent-workflow-comparison
- 开源组件：chatgpt-on-wechat、PaddleOCR、Presenton、awesome-mcp-servers 等 GitHub 仓库
