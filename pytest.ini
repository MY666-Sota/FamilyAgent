[pytest]
asyncio_mode = auto
testpaths = orchestrator/tests tests
pythonpath = .
markers =
    integration: integration tests (require MCP servers + orchestrator)
    stress: stress tests (require orchestrator)
